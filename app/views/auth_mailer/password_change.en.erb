Someone has requested that we send you password recovery link. If you didn't request this, you can safely ignore this email, your password hasn't been changed.

Here's the link:

<%= edit_user_password_url(@user, :user_id => @user.id, :token => @user.token, :escape => false) %>
